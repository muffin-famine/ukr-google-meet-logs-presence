{
  "files": [
    "src/"
  ],
  "scripts": {
    "start": "parcel serve src/index.html",
    "build": "cross-env NODE_ENV=production parcel build src/index.html --public-url .",
    "deploy": "yarn build && gh-pages -d dist -u \"Andrii Anonymous <muffin.famine@gmail.com>\""
  },
  "dependencies": {
    "papaparse": "^5.3.2"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "@types/papaparse": "^5.3.4",
    "bootstrap": "^5.2.0",
    "cross-env": "^5.2.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0",
    "typescript": "^4.8.2"
  }
}
